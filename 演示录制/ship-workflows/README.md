# 船舶工作台两案例操作回放

使用 HyperFrames 0.8.38 制作，横屏1920×1080，无配音。素材为2026-09-14实际操作的桌面窗口截图，按步骤编排，等待时间压缩；不是连续实时录屏。适合边播放边现场讲解。

两个案例：
1. 加载模拟维修单 → 识别 → 人工核对 → Agent核算 → 人工说明三项问题 → 待审批网页档案。
2. 查看模拟历史资料 → 人工确认参照与任务 → Agent编制初案 → 提交勘验 → 工具联动测算 → 人工复核 → Agent编制报告 → 桌面导出PDF。

本次真实结果：第一案例试算70776元，原表69166元，差1610元。第二案例初案19396元/133工时，修订29260元/177工时。所有价格、工艺系数和业务资料均为模拟，不能据此报价或施工。参照船自动推荐尚未实现，视频未声称已实现。

capture保留实际界面截图与本次PDF。scenes.json记录每段来源和时长；SCRIPT.md记录字幕；STORYBOARD.md记录编排。03-draft.png因当时尚在识别而弃用。

启动：npm run dev
检查：npm run check
按需导出：npx hyperframes@0.8.38 render --output renders/船舶工作台操作回放.mp4

录制准备期间：HyperFrames init在临时ship-capture-base目录生成脚手架，并自动安装其官方技能；正式项目复用生成的配置。依赖版本固定0.8.38，GSAP3.14.2下载到本项目，中文采用本机PingFang SC。

验证：静态检查0错误0警告；运行检查无控制台错误且文字对比度通过；14个时间点布局检查无问题；已逐段检查截图并在Studio核实113秒时间轴和84秒联动结果画面。官方旧animation-map脚本依赖未随CLI发布的@hyperframes/producer，未能执行；改用当前CLI的运行、布局、关键帧检查及Studio拖动验证。中文字体通过准确的本机PostScript名称加载。

已导出 renders/船舶工作台操作回放.mp4：1920×1080、H.264、30帧/秒、113秒、11654340字节、无音轨。全片解码检查通过，并抽查实际MP4的24秒画面。
