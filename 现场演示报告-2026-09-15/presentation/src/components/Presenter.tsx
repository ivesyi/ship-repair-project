import {useState,useEffect} from 'react';
import type {ChapterDef} from '../registry/types';
import './Presenter.css';
export function Presenter({chapters,cursor,jump}:{chapters:ChapterDef[];cursor:{chapter:number;step:number};jump:(c:number,s?:number)=>void}){
const [panel,setPanel]=useState(false);const [notes,setNotes]=useState(false);
useEffect(()=>{const f=(e:KeyboardEvent)=>{if(e.key==='Escape')setPanel(false);if(e.key.toLowerCase()==='m')setPanel(p=>!p);if(e.key.toLowerCase()==='n')setNotes(p=>!p)};window.addEventListener('keydown',f);return()=>window.removeEventListener('keydown',f)},[]);
return <><div className="pr-hover" data-no-advance><button onClick={()=>setPanel(true)}>目录与讲稿</button><a href="./资料.html" target="_blank" rel="noreferrer">资料出处 ↗</a><button onClick={()=>document.fullscreenElement?document.exitFullscreen():document.documentElement.requestFullscreen().catch(()=>{})}>全屏</button></div>{notes&&<aside className="pr-notes" data-no-advance><button onClick={()=>setNotes(false)}>关闭讲稿</button><p>{chapters[cursor.chapter].narrations[cursor.step]}</p></aside>}{panel&&<div className="pr-shade" role="dialog" aria-label="演示目录" data-no-advance><div className="pr-panel"><button onClick={()=>setPanel(false)}>返回演示</button><h2>现场演示目录</h2><p>方向键翻页 · M 打开目录 · N 显示讲稿 · 首尾键跳转</p><div className="pr-grid">{chapters.map((c,i)=><button key={c.id} onClick={()=>{jump(i);setPanel(false)}}>{i+1}. {c.title}</button>)}</div><button onClick={()=>{setNotes(p=>!p);setPanel(false)}}>显示／隐藏本步讲稿</button><a href="./workbench/index.html" target="_blank" rel="noreferrer">打开订单工作台 ↗</a><a href="./资料.html" target="_blank" rel="noreferrer">公开资料与原件 ↗</a></div></div>}</>}
