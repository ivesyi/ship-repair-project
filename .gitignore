# macOS
.DS_Store

# 依赖目录（所有层级）
node_modules/

# 子仓库本地历史备份（内容已并入主仓库）
.git.bak/

# 外部参考仓库（课程参考，独立 git 仓库）
classes/

# 工作区配置
.workbuddy/

# 大体积模型运行时包（超过 GitHub 100MB 单文件限制）
离线部署记录/**/*.tar.gz

# 构建产物
out/
dist/
dist-dev/
coverage/
__pycache__/
*.pyc
*.log

# 环境变量
.env
.env.*
!.env.example
